<h3><%= @product_category.name %></h3>
<dl>
    <dt><%= ProductCategory.human_attribute_name(:code) %></dt>
    <dd> <%= @product_category.code %></dd>
</dl>

<div><%= link_to t('.edit'), edit_product_category_path(@product_category) %>
| <%= link_to t('.delete'), product_category_path(@product_category), method: :delete,
                                            data: { confirm: "Deseja deletar a Categoria: #{@product_category.name}?"} %></div>

<%= link_to t('.back'), product_categories_path %>