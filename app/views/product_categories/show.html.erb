<%= flash[:notice] %>

<dl>
    <dt>Nome</dt>
    <dd> <%= @product_category.name %></dd>
    
    <dt>Código</dt>
    <dd><%= @product_category.code %></dd>
</dl>

<div><%= link_to 'Editar Categoria', edit_product_category_path(@product_category) %>
| <%= link_to 'Excluir Categoria', product_category_path(@product_category), method: :delete,
                                            data: { confirm: "Deseja deletar a Categoria: #{@product_category.name}?"} %></div>

<%= link_to 'Voltar', product_categories_path %>